<h2><%= @attraction.name %></h2>

<%= button_to "Go on this ride", action: "create", controller: "rides", options: { params: {attraction_id: @attraction.id} } %>
<br>
<% if User.find(session[:user_id]).admin %>
    <%= link_to "Edit Attraction", edit_attraction_path %>
<% end %>
